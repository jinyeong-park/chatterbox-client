var MessagesView = {

  $chats: $('#chats').append("<p>hello</>"),
//.append(MessageView.render(username, text));
  initialize: function() {
    console.log(Messages)
    // console.log(Messages[0].text);
    //loop through data
      //username = data[i].username
      //text = data[i].text
      //var newMessage = MessageView.render(username, text) => <div>userna,d</dic><div> text </div>
      //$('#chats').append(newMessage);
  },

  render: function() {
  }


};

